<h1>New Advertisements</h1>

<div class="row">
	<div class="col-md-4">
		<p>Guidelines for posts</p>
		<ul>
			<li>Make sure it rhymes.</li>
			<li>Don't use any capital letter.</li>
			<li>the use of hashtags will get you banned.</li>
		</ul>
	</div>
	<div class="col-md-8">
		<% form_for @advertisement do |f| %>
			<div class="form-group">
				<%= f.label :title %>
				<%= f.text_field :title, class: 'form-control', placeholder: 'Enter your title' %>
			</div>
			<div class="form-group">
				<%= f.label :copy %>
				<%= f.text_area :copy, class: 'form-control', placeholder: 'Enter your advertisement' %>
			</div>
			<div class="form-group">
			<%= f.label :price %>
			<%= f.number_field :price, class: 'form-control', placeholder: 'Enter the price' %>
			</div>
			<div class="form-group">
			<%= f.submit "Save", class: 'btn btn-success' %>
			</div>
		<% end %>
	</div>
</div>